name: Build Android APK

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK & System Packages
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev pkg-config perl build-essential
          sdkmanager --install "ndk;29.0.13846066"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Install npm dependencies
        run: npm ci

      - name: Build Android APK
        env:
          NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/29.0.13846066
          PATH: ${{ env.ANDROID_HOME }}/ndk/29.0.13846066/toolchains/llvm/prebuilt/linux-x86_64/bin:${{ env.PATH }}
        run: |
          npx tauri android build --apk

      - name: Upload Android APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: zyntratek-remote-manager-android-apk
          path: src-tauri/gen/android/app/build/outputs/apk/**/*.apk
